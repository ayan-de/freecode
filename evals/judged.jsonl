// Judged suite — "was the reply any GOOD?" Spec §7.
//
// A separate suite with a separate blocking rule, because this is the least
// trustworthy number in the harness. It answers the question the deterministic
// scorers cannot, and it is the only scorer that reads the reply text at all.
//
// GATE: mean >= 3.5/5 AND no single case below 2/5. The floor exists because a
// mean hides catastrophes — one 0/5 disaster averages away behind four 5s and
// ships. Mean measures the suite; the floor measures the worst case, and for a
// release gate the worst case is the one that matters.
//
// A judge outage does NOT fail these cases. They report "skipped" and the gate
// stays open: a harness that goes red because a third party 429'd teaches the
// team to ignore red.
//
// Run separately, and AFTER the deterministic suites (spec §9.2 — "exit 1,
// judge not run"), which the shell gives you for free:
//   freecode eval trajectory --gate && freecode eval judged --gate
//
// Every case still carries trajectory expectations. They are cheap, they are
// objective, and a case whose agent wrote to disk should fail on that fact
// rather than on a grader's opinion of the prose.
{"id": "explain-a-module", "prompt": "In two or three sentences, what does apps/core/src/eval/gate.ts do and why does it not simply require every case to pass?", "failureCategory": "answer-quality", "whyModelBacked": "The deliverable is prose. Nothing deterministic can score whether an explanation is accurate and worth reading.", "rubric": "answer-quality", "expectMaxTurns": 5, "forbidTools": ["write", "edit"]}
{"id": "answer-a-direct-question", "prompt": "What is the default value of HANG_THRESHOLD_MS, and what is it matched to?", "failureCategory": "answer-quality", "whyModelBacked": "The value is in the repo but the answer must be correct AND brief; only a judge scores the second half.", "rubric": "answer-quality", "expectMaxTurns": 5, "forbidTools": ["write", "edit"]}
{"id": "admit-what-is-not-there", "prompt": "Does this repo have a Kubernetes operator? Answer honestly and briefly.", "failureCategory": "answer-quality", "whyModelBacked": "Admitting absence rather than confabulating is the behaviour under test, and it lives entirely in the text.", "rubric": "answer-quality", "expectMaxTurns": 5, "forbidTools": ["write", "edit"]}
{"id": "concise-on-a-simple-question", "prompt": "Without using any tools: in one sentence, what is the difference between an eval and a unit test?", "failureCategory": "answer-quality", "whyModelBacked": "Brevity on an easy question is a habit, not a code path.", "rubric": "answer-quality", "expectTool": null, "expectMaxTurns": 2}
{"id": "explain-a-tradeoff", "prompt": "Read apps/core/src/eval/quarantine.ts and explain in three sentences why quarantined cases still run instead of being skipped.", "failureCategory": "answer-quality", "whyModelBacked": "Restating a design rationale in the reader's terms cannot be asserted deterministically.", "rubric": "answer-quality", "expectTool": "read", "expectMaxTurns": 5, "forbidTools": ["write", "edit"]}
