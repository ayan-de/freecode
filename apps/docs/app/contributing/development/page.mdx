---
title: "Development setup"
description: "Get the monorepo running locally."
---

# Development setup

Install once at the root, then run the app you are working on. Core is spawned
from disk in dev, so changes to it are picked up by every frontend.

## On this page (coming soon)

- Prerequisites and `pnpm install`
- Running the TUI, web, and docs in dev
- Running tests, lint, and type checks
- Debugging core independently of a frontend
