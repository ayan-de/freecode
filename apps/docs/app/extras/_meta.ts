import type { MetaRecord } from "nextra";

export default {
  index: "Overview",
  "shell-output-compression": "Content-aware shell output compression",
  "line-number-prefixes": "Removing line-number prefixes from file reads",
  "guidance-compression": "Compressing recurring tool guidance",
  "background-completions": "Batching background completions inline"
} satisfies MetaRecord;
