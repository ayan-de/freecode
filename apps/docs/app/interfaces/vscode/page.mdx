---
title: "VS Code extension"
description: "FreeCode inside VS Code as a React webview."
---

# VS Code extension

The VS Code extension is an extension host plus a React webview. The host spawns
the same core backend the terminal UI uses and forwards JSON-RPC between it and
the webview, so the agent behaves identically — it just renders in an editor pane.

## On this page (coming soon)

- Installing and opening the panel
- How the extension host, the webview, and core fit together
- Diffs and file navigation inside the editor
- Known differences from the terminal UI
