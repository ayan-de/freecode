---
title: "Custom commands"
description: "Define your own slash commands as prompt templates."
---

# Custom commands

Prompt commands are slash commands whose logic is a prompt. FreeCode ships
`/init` as a built-in and loads your own from user and project scopes, so
`/deploy-check` or `/adr` can be part of a repo the same way a script is.

## On this page (coming soon)

- Built-in, user, and project scopes
- The file format and frontmatter
- Passing arguments and using `argHint`
- Prompt commands vs UI commands vs skills — which to reach for
