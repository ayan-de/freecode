---
title: "Skills"
description: "Reusable instruction sets loaded from SKILL.md files."
---

# Skills

A skill is a markdown file with frontmatter that packages a workflow — a review
checklist, a deploy runbook, a house style. FreeCode discovers skills from user
and project directories, advertises them to the model, and injects the body only
when the `skill` tool loads one.

## On this page (coming soon)

- Where skills are discovered from
- SKILL.md frontmatter: `name`, `description`
- How skills are advertised vs injected (progressive disclosure)
- Writing a good `description` so the model picks the right skill
- Listing what is available with `/skills`
