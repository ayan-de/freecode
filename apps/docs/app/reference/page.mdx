---
title: "Reference"
description: "Exhaustive listings: commands, settings, variables, protocol."
---

# Reference

Reference pages are complete and boring by design. If a guide explains *why*, the
reference tells you *exactly what* — every flag, every key, every method
signature.

## On this page (coming soon)

- Every CLI command and flag
- Every `settings.json` key
- Every `FREECODE_*` environment variable
- Every IPC method and stream event
- Every hook event and its payload
