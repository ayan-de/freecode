---
title: "Hook events"
description: "All 14 events with payloads and return contracts."
---

# Hook events

Each entry gives the event name, when it fires, its input payload, what it may
return, and whether returning a block actually stops the action.

## On this page (coming soon)

- Payload and return type per event
- Blocking vs modifying semantics
- Matcher syntax
- Command executor: stdin, stdout, exit codes
- Callback executor
