---
title: "settings.json"
description: "Full schema for project and user settings."
---

# settings.json

Settings are read from `.freecode/settings.json` (project) and
`~/.freecode/settings.json` (user), with project taking precedence.

## On this page (coming soon)

- `permissions` — `allow`, `ask`, `deny`
- `hooks` — event definitions and matchers
- `memory` — `autoExtract`, `extractEveryNRuns`
- Provider and model defaults
- MCP server definitions
- Precedence rules and merge semantics
