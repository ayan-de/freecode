---
title: "CLI entrypoint & subcommands"
description: "The yargs entrypoint and its subcommands."
---

# CLI entrypoint & subcommands

Stub — not yet written. Lives in `apps/core/src/cli.ts` (yargs entrypoint) and
`apps/core/src/cli/` (subcommands: `mcp`, `session`, `web`, …).
