---
title: "Slash commands"
description: "How slash commands are listed and resolved."
---

# Slash commands

Stub — not yet written. Lives in `apps/core/src/commands/`. Exposed over IPC via
`commands.list` / `commands.resolve`.
