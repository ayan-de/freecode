---
title: "Getting started"
description: "Install FreeCode, connect a provider, and run your first session."
---

# Getting started

Four short pages take you from nothing installed to a working agent session:
[install the binary](/getting-started/installation), [run your first
session](/getting-started/quickstart), [pick a provider and set its
key](/getting-started/providers), and [configure the
defaults](/getting-started/configuration) you want on every project.

## On this page (coming soon)

- What you need before you start (a shell, a git repo, one provider API key)
- The difference between the released binary and running from the monorepo
- Where FreeCode keeps its state (`~/.freecode/`) and per-project config (`.freecode/`)
