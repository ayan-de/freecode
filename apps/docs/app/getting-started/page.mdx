---
title: Getting started
description: Install and run FreeCode locally.
---

# Getting started

> **Stub.** This page will describe how to install FreeCode, configure providers, and run the TUI.

## What's coming

- Prerequisites (Node.js, a provider API key).
- `pnpm install` at the monorepo root.
- `pnpm --filter tui build:bun` to build the release binary.
- First-run conversation.
