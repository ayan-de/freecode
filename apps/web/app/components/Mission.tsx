import { Target } from "lucide-react";

export function Mission() {
  return (
    <section id="mission" className="w-full max-w-4xl mx-auto pt-4 pb-12 text-left">
        <p className="text-base md:text-lg text-muted-foreground leading-relaxed">
          FreeCode is a harness that is trying to push the frontier of intelligence. 
          We believe that the next leaps in agent capabilities will come from the harness level. 
          The model is just the intuitive core of an agent, and the harness around it exists to 
          extract as much useful work out of that core as possible. Scaling test time compute 
          via agent swarms, increasing speed through parallelism, solving continual learning 
          through memory. We publish everything publicly, and will remain open source and open 
          research. At the same time, we strive to be a good general purpose dev tool for day 
          to day work.
        </p>
    </section>
  );
}
